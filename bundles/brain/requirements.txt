# brain bundle — aggregate Python dependencies (sqlite3 is stdlib).
# NOTE: fastembed (RAG, onnx) + pypdf/openpyxl need no PyTorch, but `docling`
# (PPTX/DOCX parsing) pulls torch/transformers, so the installed set is ~1.3 GB.
# Install into the skills' OWN isolated venv (not your project's) with uv:
#   ./install.sh --bundle brain --deps            # -> <project>/.claude/venv
#   ./install.sh --bundle brain --deps --user     # -> ~/.claude/venv (ONE shared env; avoids 1.3 GB per project)
#   npx github:onetest-ai/applied_skills init --bundle brain --deps [--user]
# Or run any script with zero install, deps materialized + cached on demand:
#   uv run --with-requirements requirements.txt python <script>

# narrative lane (knowledge-index): hybrid RAG over SQLite
sqlite-vec
fastembed

# parsing (corpus-taxonomy-extraction): document extraction
docling      # PPTX/DOCX — pulls torch/transformers (the heavy dep)
pypdf        # PDF — no torch
openpyxl     # XLSX — no torch

# numbers lane (tabular-semantic-layer): Excel -> facts
pandas
pyarrow
