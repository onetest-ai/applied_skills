# brain bundle — aggregate Python dependencies (sqlite3 is stdlib).
# NOW GENUINELY TORCH-FREE: docling is retired (it dragged torch/transformers ~1.3 GB).
# The parse stack is PyMuPDF (PDF text + page render + table extraction) + LibreOffice
# (soffice) for office→PDF + a vision model for visual pages. So the installed set is
# small (~200 MB, mostly onnxruntime for fastembed). LibreOffice is a SYSTEM dep (not pip),
# needed only for .pptx/.docx; PDFs need only PyMuPDF.
# Install into the skills' OWN isolated venv with uv:
#   ./install.sh --bundle brain --deps            # -> <project>/.claude/venv
#   ./install.sh --bundle brain --deps --user     # -> ~/.claude/venv (shared)
#   uv run --with-requirements requirements.txt python <script>   # zero-install

# narrative lane (knowledge-index): hybrid RAG over SQLite — no torch
sqlite-vec
fastembed

# parsing (corpus-taxonomy-extraction + visual-parse): text layer, page render, tables — no torch
pymupdf
openpyxl     # XLSX structure dump

# numbers lane (tabular-semantic-layer): Excel -> facts
pandas
pyarrow

# tool layer (mcp/brain): the MCP server is stdlib-only stdio JSON-RPC — NO extra dep.
# System (not pip): LibreOffice `soffice` for rendering/parsing .pptx/.docx.
